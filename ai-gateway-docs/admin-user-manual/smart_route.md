---
title: 智能路由
description: 介绍 1Panel AI 网关智能路由的样本管理、决策日志与统计，实现请求自动分类与模型调度。
---

## 1 概述

智能路由用于根据请求内容自动匹配并选择合适的模型，实现请求的分类转发与模型调度。系统通过预置的分类样本来识别请求的复杂度，结合本地规则、向量相似度等来源作出路由决策，并将每次决策记录到决策日志中，最终通过统计页面展示整体分布情况。

智能路由包含三个模块：

- **样本管理**：维护用于请求分类的样本数据，支持设置样本标签以及单样本阈值。
- **决策日志**：可查看每一次请求的路由分类、决策来源、选中模型和置信度等相关信息。
- **统计**：支持按照标签、来源、模型、Token 维度查看决策分布情况。

三者的关系为"样本 → 决策 → 统计"：样本提供了判定依据，决策是依据样本实时完成的路由选择，统计则对大量决策结果做汇总呈现。其中**样本管理**是核心，样本的质量直接影响智能路由的准确度。

## 2 样本管理

样本管理用于维护请求分类所需的路由样本。可在此查看、添加、批量导入样本，构建样本向量，并通过"决策预览"验证路由效果。系统根据样本与请求的相似度对请求进行分类，分类结果决定请求被路由到简单或复杂模型。

### 2.1 样本列表

进入智能路由后默认显示样本管理页。样本列表按分页展示全部样本，包含标签、样本文本、向量与操作等信息。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image1_Sample_Management.png" alt="样本管理列表"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 1  样本管理列表</div>

### 2.2 添加样本

要让智能路由"认识"你的典型请求，就需要把这类请求作为**样本**录入系统。样本是所有判定的参照物，因此添加样本是使用智能路由的第一步。点击「**添加样本**」按钮，在弹出的表单中填写以下内容：

- **标签**：选择该样本的分类标签。标签决定了该样本代表"简单"还是"复杂"等类别，是后续判定归类的依据。

- **样本文本**：填写用于匹配的文本内容，最大支持 65536 个字符。文本应尽量贴近真实请求的典型表达，覆盖越全，判定越准。

- **单样本单值**：为该样本补充一个单值，可通过「+」「-」进行调整，用于提升匹配精度，可理解为对该样本重要程度或特征的量化补充。

- **备注**：填写对该样本的补充说明（选填），便于日后理解该样本的用途。

- **保存后生成向量**：勾选后，保存样本的同时自动生成对应的向量。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image2_Decision_Preview.png" alt="添加样本"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 2  添加样本</div>

### 2.3 批量添加

当样本数量较多（例如历史请求已积累了成百上千条）时，逐条录入效率极低。批量添加可一次性导入多条样本，快速建立样本库，从而缩短"从无到有"的准备周期。

点击「**批量添加**」按钮，可一次导入多条样本。批量添加表单包含标签、样本文本等字段，并支持「保存后生成向量」选项，便于一次性录入大量样本，无需逐条操作。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image3_batch_add.png" alt="批量添加"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 3  批量添加</div>

### 2.4 决策预览

单击 **决策预览**，可输入一条请求样本，先查看路由决策的预判结果，确认无误后再决定是否添加为正式样本，以便在正式启用前及时发现样本或标签配置的问题，避免不当策略上线。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image4_Add_Sample.png" alt="决策预览"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 4  决策预览</div>

### 2.5 构建向量

计算机无法直接比较两段文字的"相似度"，需要先把文本转换为**向量**（一组数值），才能进行语义匹配。样本未经向量化，就无法参与智能判定。因此构建向量是样本真正"生效"的必经步骤。

页面提供以下向量构建操作：

- **构建选中向量**：勾选列表中的样本后，为选中的样本构建向量。

- **构建全部向量**：为列表中的全部样本构建向量。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image5_build_vector.png" alt="构建向量"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 5  构建向量</div>

向量构建完成后，样本才能参与智能路由的匹配判定。

### 2.6 编辑与删除

- **编辑**：在「操作」列点击编辑，可修改样本的标签、样本文本、单样本单值及备注等内容。当业务表达变化时及时修改样本，可保证判定依据不过时。

- **删除**：移除不再需要的样本。删除后将不再参与路由判定，请确认后操作。

## 3 决策日志

智能路由的决策过程由系统自动完成，有别于人工可逐条核对的固定规则。若仅查看决策结果，通常难以判断其选型依据；一旦出现选错模型或路由异常的情况，缺乏记录将难以定位原因。**决策日志**详细留存每一次决策的完整信息，使路由过程可追溯、可审查，并为后续的策略优化提供依据。

### 3.1 决策日志筛查

列表顶部提供筛选与搜索能力，可按**路由分类、决策来源、请求类型、选中模型**过滤，也可按 **Request ID / 模型 / 文本** 搜索，便于快速定位某次决策。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image6_Decision_Logs.png" alt="决策日志列表"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 6  决策日志列表</div>


### 3.2 决策日志查看

点击某条记录的「查看」，可打开决策详情面板，查看该次请求的完整决策信息，包括请求的规范化文本、实际置信度、决策来源等，以及用于本次匹配的 **Top K 命中样本**（即参与判定的最相关的若干条样本）。通过详情可清楚了解本次路由决策的依据与结果，例如当请求被误分时，可据此判断是样本缺失还是阈值设置不当。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image7_Decision_Detail.png" alt="决策详情"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 7  决策详情</div>


## 4 统计

样本与路由策略是否有效，无法凭主观判断，需以实际数据为依据。统计页面将大量决策结果予以汇聚，用于回答"路由效果表现如何、哪些方面需要优化"等问题，从而为样本与策略的持续迭代提供量化依据。

- **总览指标**：筛选区下方以卡片形式展示所选时间范围内的核心指标，包含决策数、真实请求数、失败数、总 Token、平均 Token、累计耗时，直观展示智能路由决策运行的整体概况。
- **分布表格**：总览卡片下方按多个维度展示用量分布，分布维度包含按标签分布、按来源分布、按模型分布、按总 Token 分布。可从整体上观察路由分类、请求来源、模型选用等维度的占比构成。通过上述数据可识别规律与异常，例如某类样本占比过高、某模型的失败率出现异常等，从而为样本优化提供参考。

<img style={{display:"block",margin:"16px auto",maxWidth:"100%"}} src="/img/smart_route/image8_Statistics.png" alt="统计概览"/>

<div style={{textAlign:"center",color:"#8a8f99",fontSize:"13px",margin:"6px 0 20px"}}>图 8  统计概览</div>

:::note[说明]
统计页支持切换时间维度查看不同周期的数据，包括近 24 小时、近 7 天与近 30 天。
:::

## 5 注意事项

- **样本质量直接决定路由准确度**：智能路由依赖样本文本进行匹配，请确保样本标签准确、内容覆盖到位，并定期补充与更新。

- **样本需向量化后才能参与判定**：新增或修改样本后，需构建对应向量，否则无法进入智能匹配。

- **使用决策预览验证效果**：正式调整样本策略前，建议先通过"决策预览"验证路由结果是否符合预期。

- **谨慎删除样本**：删除样本后即不再参与路由判定，确认无需使用后再执行。
